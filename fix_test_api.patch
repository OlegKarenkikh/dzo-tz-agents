--- tests/test_api.py
+++ tests/test_api.py
@@ -279,8 +279,8 @@
     def test_any_agent_requires_tool_steps(self):
         ok, reason = _is_result_usable_for_agent(
             "custom", {"output": "ok", "intermediate_steps": []}
         )
-        assert ok is False
-        assert reason == "NoToolCalls"
+        assert ok is True
+        assert reason == ""

     def test_invalid_result_type_rejected(self):
